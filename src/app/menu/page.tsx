// import React, { useEffect, useState } from 'react'

const Menu = () => {
  // const [products, setProducts] = useState([]);

  // useEffect(() => {
  //   const loadProducts = async () => {
  //     await initializeData(); // Ensures initial data exists
  //     const productList = await fetchProducts(); // Fetch updated product list
  //     setProducts(productList);
  //   };

  //   loadProducts();
  // }, []);
  return (
    <div>
      <p>Im menu</p>
    </div>
  )
}

export default Menu